{
  "name": "percolator-launch",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @percolator/app dev",
    "build:app": "pnpm --filter @percolator/app build",
    "build:core": "pnpm --filter @percolator/core build",
    "test": "pnpm -r test",
    "test:unit": "pnpm -r test:unit",
    "test:integration": "pnpm --filter @percolator/server test:integration",
    "test:e2e": "playwright test",
    "test:coverage": "pnpm -r test:coverage"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "msw": "^2.12.10",
    "playwright": "^1.58.2",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "@solana/spl-token": "^0.3.11",
    "@solana/web3.js": "^1.98.4",
    "pg": "^8.18.0"
  }
}
